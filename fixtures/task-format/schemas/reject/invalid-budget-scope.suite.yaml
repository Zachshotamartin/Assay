format_version: "1.0"
id: invalid-scope
title: Suite budgets require suite scope
include: [tasks/*.task.yaml]
budgets:
  dollars: { limit: 1, aggregate: median, scope: task }
variants:
  baseline: { adapter: simulated, model: synthetic/deterministic-1 }
